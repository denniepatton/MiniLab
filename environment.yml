name: minilab
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    - pyyaml>=6.0
    - httpx>=0.27
    - pydantic>=2.8
    - python-dotenv>=1.0
    - feedparser>=6.0  # For parsing arXiv RSS feeds
    - networkx>=3.0  # For knowledge graph operations
    - matplotlib>=3.7  # For graph visualization
    - jupyter>=1.0  # For notebook interface
    - pytest>=7.0
    - ruff>=0.1
    - mypy>=1.0
    - black>=23.0
