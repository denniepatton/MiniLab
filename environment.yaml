name: minilab
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - graphviz
  - pip:
    - pyyaml>=6.0
    - httpx>=0.27
    - pydantic>=2.8
    - python-dotenv>=1.0
    - anthropic>=0.34
    - sentence-transformers>=3.0  # Updated for pyarrow compatibility
    - datasets>=3.0  # Required for pyarrow 22.x compatibility
    - faiss-cpu>=1.7
    - numpy>=1.24
    - feedparser>=6.0
    - tavily-python>=0.3
    - networkx>=3.0
    - matplotlib>=3.7
    - pandas>=2.0
    - aiofiles>=23.0
    - pytest>=7.0
    - pytest-asyncio
    - ruff>=0.1
    - mypy>=1.0
    - reportlab>=4.0  # PDF generation with Arial font support
